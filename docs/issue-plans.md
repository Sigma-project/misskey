# 未解決 issue の実装計画

調査日: 2026-09-07（Asia/Tokyo）

対象: `Sigma-project/misskey` の open issue 全2件。PRは対象外。issue本文・コメントを GitHub から取得し、各担当で並列に調査・設計する。

| Issue | 計画 | 状態 |
| --- | --- | --- |
| [#15 JXL 移行の regression](https://github.com/Sigma-project/misskey/issues/15) | [計画](issue-15-plan.md) | [PR #20](https://github.com/Sigma-project/misskey/pull/20) マージ済み・全39 CI成功 |
| [#18 既存の90日DBクリーンアップに添付ファイル削除を追加](https://github.com/Sigma-project/misskey/issues/18) | [計画](issue-18-plan.md) | 実装・統合検証・正式レビュー完了。PR/CI状況はissue内のPRを参照 |

## 計画作成時の基準と作業範囲

- 計画基準は取得時の `origin/master`、`2b006ee066e6360957a0c37c42569a23273c35ff`。
- 作業ディレクトリの HEAD は `5b730af8d127beb34cfcc29c2f6258b1119c4243` であり、旧版での初期調査結果をそのまま最新の事実として使用しない。最新コードは `git show origin/master:<path>` で参照した。作業ツリーの更新・実装は行わない。
- 設計比較は Claude Fable 5.1 (`claude-fable-5-1`) を `--permission-mode plan` 明示で実行する。指定モデルへの接続確認は成功。サンドボックス内の試行はタイムアウトし、許可されたサンドボックス外の試行で応答した。
- 今回は計画文書作成のみ。実装許可は未取得。実装後の Opus 5・subagent レビュー、CI、PR・マージは実装許可後の工程であり、実施済みとは扱わない。

## ユーザー判断

2026-09-07: 「立っている issue についてそれぞれ並列で実装計画を立てて」と依頼された。対象は上記2件。理由の提示なし。実装開始の許可ではない。

#18 の期間定義、保護対象、有効化方法に関する初回の質問は、下記スコープ変更により取り下げた。

2026-09-07 **ユーザー判断**: Claude の設計・議論にタイムアウトを設けない。ユーザーが示した理由は「60 秒は無理だと思う」。状態確認の短い待機は継続するが、経過時間を理由に実行を中断しない。#15 は長い待機後に指定モデルの設計回答が得られたため、利用不可とは断定しない。先に提示したモデル代替の質問は保留とし、指定モデルで共同設計を進める。

2026-09-07 **ユーザー判断**: #18 の issue を変更し、既存の90日DB削除にファイル削除を追加する改修とする。理由は既存の削除機能があるため。既存の削除条件・保護条件・期間設定・有効化設定を引き継ぎ、1ヶ月化や新規のフォロー保護は今回の範囲から外す。この指示は issue 更新と計画変更の許可であり、実装許可ではない。

2026-09-07 **ユーザー判断**: 更新された AGENTS.md に従い、以後の Claude 起動ではセッションを保存し、`--no-session-persistence` を使用しない。許可形式 `claude --permission-mode plan --model claude-fable-5-1 --tools '' --output-format json` にプロンプト・再開引数を末尾追加する。理由は設計・議論・レビューを再開可能にするため。過去の永続化無効の実行履歴は改変せず、必要な文脈を保存セッションへ引き継ぐ。フックの信頼状態は確認していないため、自動拒否が有効とは報告しない。

2026-09-07 **ユーザー判断**: #15 の tutorial は `.jxl` ファイルを生成して追加する。理由の提示なし。既存 WebP を入力として ai.jxl / natto_failed.jxl を追加する計画に更新した。実装許可は未取得。

## 並列実装時の依存関係

計画作成は独立して進められる。実装する場合は issue ごとに最新 master からブランチを作る。#15 の画像処理と #18 のファイル回収は Drive 周辺の検証で接点があるため、先行PRの取り込み後に回収対象の派生画像・動画を再確認する。機能ごとのコミットと、コード生成コマンドごとの別コミットを維持する。

## 文書の検証

`shipping-misskey-change` を最新 master から読了。今回の変更は Markdown のみのため、コード lint・型検査・API生成・migration検査・SPDX・locale・CHANGELOG・実装レビューは非該当。文書の内容、参照先、変更ファイルと `git diff --check` を確認する。実装テストの成功を主張しない。
